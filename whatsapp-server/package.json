{
  "name": "whatsapp-server",
  "version": "2.0.0",
  "description": "Conversational automation platform — WhatsApp + Wix + AI",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "build:ui": "cd admin-ui && npm install && npm run build",
    "build": "npm run build:ui",
    "postinstall": "echo 'Run: npm run build:ui to build the admin UI'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "csv-parse": "^5.5.6",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.3",
    "libphonenumber-js": "^1.11.12",
    "multer": "^2.0.0",
    "node-fetch": "^3.3.2",
    "pino": "^9.4.0",
    "pino-pretty": "^11.2.2"
  },
  "engines": {
    "node": ">=20"
  }
}
